"""Matrix Heavy Extractors (Venv 3, Phase 2 — currently SKELETON).

Houses extractors with pillow/torch conflicts that can't coexist with
hindsight-api-slim in the ingestion venv. Called via HTTP from the
ingestion-worker.

See pyproject.toml for activation steps.
"""

__version__ = "0.1.0"
__phase__ = "skeleton"
