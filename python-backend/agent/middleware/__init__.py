"""Agent Middleware — Cross-Cutting Concerns (exec-10 Phase 5).

Ordered pipeline inspired by deer-flow + LangChain built-in middleware.
Each middleware wraps model calls and/or tool calls with additional logic.
"""
