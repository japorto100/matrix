# 2026-04-23 — Smart-Routing ADR-001 + 4 Backend-Gates

## Zusammenfassung

Contrarian-Review von `exec-16 §2.D` (smart cheap-vs-strong routing) durchgezogen,
ADR-001 geschrieben, und 4 von 6 Rollout-Gates als Code gelandet + getestet +
gepusht. Rollout von `smart_routing.enabled=true` pro User bleibt blockiert
bis die zwei Frontend-Gates (G5 User-Indicator, G6 Control-UI) + P1
Refactor fertig sind.

## Tasks (in Reihenfolge)

| ID | Status | Commit | Beschreibung |
|---|---|---|---|
| #65 | ✅ DONE | `7163d75` | Cluster H: exec-16 §2.D smart-routing holistic review — sota-contrarian `stakes=high` + ADR-001 |
| #84 | ✅ DONE | `0a59a76` | ADR-001 G1: EN+DE keyword set (~140 words) + hyphen-tokenizer |
| #86 | ✅ DONE | `5061586` | ADR-001 G3: 60s TTL in-process cache on `get_user_smart_routing_config` |
| #85 | ✅ DONE | `57400f4` | ADR-001 G2: credential pre-flight vor Model-Switch |
| #87 | ✅ DONE | `dc539df` | ADR-001 G4: A/B harness routing dimension (Migration 027, 3 neue Columns + Index, `_mark_routing` helper, ctx+state threading) |
| — | ✅ | `ddcd6fe` | docs(journal): session log + open-tasks status update |
| #66 | ✅ DONE | `c45d639` | Cluster H: exec-17 C9 ADR — ADR-002 tracing/audit parallel stores + `LLM_REQUEST` audit_log removal (~10 LOC) |
| #90 | ✅ DONE | `89ed88d` | ADR-001 P1: router_node refactor — smart-routing als eigener Graph-Node statt llm_node side-effect |
| — | ✅ | `6407eb3` | docs(journal): 2026-04-23 final update (interim) |
| #75 | ✅ DONE | `30e109c` | Cluster K: exec-14-DSPy `sota-contrarian stakes=high` → ADR-003 (D-1 reframing, D-2/D-3 deferred, Phase-(-1) PoC gate, LLMSelector architectural-match-conditional) |
| — | ✅ | `f1123e3` | docs(journal): ADR-003 entry |
| #67 | ✅ DONE (partial) | `753d2ba` | Cluster H: exec-harness §4g.4 **eval_id wiring** backend — score_session/evaluator/HTTP endpoint nehmen optional `eval_id`, flow zu `ab_experiments.harness_eval_id`. Pareto dashboards (frontend) + fitness-weights tuning (Phase-D research) bleiben offen innerhalb §4g.4. |
| #88 + #89 | ✅ DONE | `14dbcdf` | Smart-routing G5 (user-visible indicator, amber pill + tooltip in agent-chat) + G6 (Control-UI SmartRoutingSection panel mit toggle/cheap-model/thresholds + disclosure banner). Backend: neue `/api/v1/control/user/llm/smart-routing` GET/PUT endpoints + message_metadata carries routingUsed/Reason/Picked. **ADR-001 rollout gate 6/6 + P1 complete.** |
| #62 | ✅ DONE | `e0bf944` | exec-06 Phase 5: title-gen async dispatch in runner + idempotent persist_session_title + CompressionIndicator (with compaction vs compression vs archive distinction per user-feedback) + `/api/agent/compression-status` BFF route. |
| #69 | ✅ DONE | `c6c2ecf` | exec-a2fm ML-router research — A²FM paper downloaded (arxiv 2510.12838v3, OPPO team) + research writeup with 4 lift-options L1-L4 + exec-a2fm Phase-2 re-sequenced. |
| #75 | ✅ DONE | `30e109c` | exec-14-DSPy contrarian review → ADR-003 (D-1 reframing "add not replace", D-2/D-3 deferred on D-7, Phase-(-1) PoC gate, LLMSelector architectural-match-conditional). |
| #67 L1 | ✅ | `c368752` | Harness mode_analysis.py (post-hoc labeling of audit_events by mode) — for Phase-2 gate-data. |
| #56 | ✅ | — | claude-merge VERIFY-GATES shell+compose re-verified. |

Total: ~17 Commits, ~1900 LOC inkl. Tests, 413 tests green, `ruff` clean, biome clean. Plus 3 ADRs (001 smart-routing, 002 tracing/audit, 003 DSPy), 1 paper research (A²FM), 2 new MEMORY entries (harness-naming, compression-taxonomy) + 2 new feedback memories (gitnexus-reindex, cross-context-before-coding).

### Smoke-test (E2E, live dev-stack)

Stack up: postgres (podman) + nats (podman) + go-appservice :29318 + python-agent :8094 + frontend_merger :3003. Tuwunel/matrix intentionally skipped (not needed for smart-routing rollout gates).

**Verified green:**
- Migration 027 applied: `agent.ab_experiments.routing_{used,reason,picked_model}` columns + `ix_ab_experiments_routing` index present.
- Migration 026 applied: `agent.user_llm_settings.smart_routing` JSONB column present.
- `GET /api/v1/control/user/llm/smart-routing` direct → `{"user_id":"default-dev-user","smart_routing":{}}`.
- `PUT` with full config → 200 OK, whitelist normalization drops extra keys, only allowed fields persist.
- `GET /api/v1/agent/context/compression-status?model=X` → correct thresholds `{0.8, 0.85, 0.95}`, engine `DefaultContextEngine`.
- **Frontend BFF roundtrip:** `/api/control/user/llm/smart-routing` PUT via :3003 → Go :29318 (new route added this session, commit `968f432`) → Python :8094 → Postgres → DB write verified via `SELECT`.
- **DOM smoke on `/control/api`:** SmartRoutingSection renders with Switch (aria-label "Enable smart routing"), cheap-model input, max_simple_chars / max_simple_words inputs, Save button (disabled initially). Panel correctly appears between Model Explorer and Utility Models sections.

**Infra friction observed:**
- Postgres container exits 137 / drops connections on subprocess spawns (rootlessport race — saved as `project_postgres_rootlessport_race` memory). Workaround: `podman restart postgres`, sometimes twice.
- `go-appservice` requires NATS up (failed silently without it in dev-stack.sh log — must start NATS alongside).
- Chat drawer (GlobalChatOverlay) doesn't open reliably under `next dev` due to bundle-parse latency. **Not a code bug** — in production build (`bun run build && bun start`) the drawer opens instantly, Chrome-DevTools `evaluate_script` works without timeout, and CompressionIndicator is verified live with aria-label `Context compression: normal` + tooltip `Context usage normal — plenty of room.` + 0% text.

**One more commit this session:** `968f432` adds Go route `/api/v1/agent/context/compression-status` so the BFF proxy reaches Python. Local commit; push blocked by expired GitHub PAT (user needs to re-run `gh auth login -h github.com` next session).

## Was wurde gebaut + warum

### ADR-001 (`docs/superpowers/findings/2026-04-23-adr-smart-routing-rollout-gate.md`)

**Problem:** In früherer Session (2026-04-20) landete ein konservativer keyword-heuristic Router (`python-backend/agent/llm/smart_routing.py`) mit Migration 026 als "Stufe-0 MVP", geflaggt als `NEEDS HOLISTIC REVIEW`. 6 offene Design-Fragen. Gated off default; aber: bevor er pro-User angeschaltet werden kann, brauchte es eine adversarial walk-through damit kein silent correctness/compliance/observability bug live geht.

**Was:** `sota-contrarian stakes=high` Review → Written ADR mit Verdict pro Frage + Rollout-Gate (G1-G6 + P1 Inversion-Target). Critical findings waren:
1. **Silent vendor substitution** (User wählt Anthropic → simple turn silent auf OpenAI cheap) — GDPR/DPA-Risiko.
2. **`get_user_smart_routing_config` öffnet DB-Conn per iteration-0 turn für ALLE User**, auch `enabled:false` — Latenz + Pool-Pressure.
3. **Keyword-Liste war English-only** → complex DE query ("analysiere mein Portfolio") routet CHEAP, trivial EN ("please review this") bleibt PRIMARY — heuristic ist für DE-User invers zum behaupteten conservative-bias.
4. **Credential-Pool mismatch** — nach state["model"]=cheap acquire(cheap_provider) returns None wenn User nur primary-provider creds hat → state.api_key (wrong key) durchgereicht → 401.
5. **A/B Harness contamination** — self-selected SQL-enabled cohort confounds `langgraph vs simple_loop` fitness measurement.
6. **Deprecation-Cliff** auf literal `cheap_model: "gpt-4o-mini"` + silent except swallows future 404.

### G1 — Bilingual Keyword Set + Hyphen-Tokenizer (commit `0a59a76`)

**Files:** `python-backend/agent/llm/smart_routing.py`, `tests/agent/llm/test_smart_routing.py`.

- `_COMPLEX_KEYWORDS`: ~80 EN → ~140 EN+DE. DE flection forms (analysiere/analysieren/analysiert/analyse, optimiere/optimieren/optimierung, etc.). Verb-Formen explizit weil Set-Intersection exakt ist (kein Stemmer).
- Tokenizer splittet jetzt Hyphen-Compounds: `"Sharpe-Ratio"` → `{sharpe, ratio}`. Ohne das slippten Domain-Terms durch.
- 9 neue Tests: DE reasoning/trading/research/coding kill cheap-route, DE small-talk routes, hyphen compound matches, alle flection-Formen von `analysieren`.

### G3 — Config Cache (commit `5061586`)

**Files:** `python-backend/agent/security/credentials.py`, `tests/agent/security/test_smart_routing_cache.py` (neu).

- In-process dict cache keyed on `user_id`, 60s TTL via `time.monotonic()`, negatives werden gecached (häufigster Fall: User hat `{}`).
- Stale-on-error: bei DB-Fehler wird letzte gecachte Value weitergegeben statt auf None zu thrashen.
- Test-helper `_smart_routing_cache_clear()` für pytest-fixture teardown.
- 7 neue Tests: cache-hit, negative-cache, TTL-expiry, per-user-isolation, empty-user-id-skip, missing-DB-url, stale-on-error.

### G2 — Credential Pre-Flight (commit `57400f4`)

**Files:** `python-backend/agent/security/credentials.py` (+`user_has_provider_credential`), `python-backend/agent/graph/nodes/llm_node.py` (wire-point), `tests/agent/security/test_credential_preflight.py` (neu).

- Neue Non-mutating Funktion `user_has_provider_credential(user_id, provider) -> bool` wrapping `get_user_api_key`.
- `llm_node.py` smart-routing Block: vor `state["model"] = cheap_model` check: wenn `cheap_provider != primary_provider` → `user_has_provider_credential(user_id, cheap_provider)`. Miss → keep primary, `routing_reason="no_cheap_credentials"`.
- Same-provider-switches skippen den Check (trust primary creds).
- 5 neue Tests: key present/missing, empty inputs, empty-string key.

### G4 — A/B Harness Routing Dimension (commit `dc539df`)

**Files:** `python-backend/alembic/versions/027_ab_experiments_routing_dim.py` (neu), `agent/context.py`, `agent/graph/state.py`, `agent/graph/runner.py`, `agent/graph/nodes/llm_node.py`, `agent/runners/dispatcher.py`, `tests/agent/runners/test_mark_routing.py` (neu).

- **Migration 027:** `agent.ab_experiments` bekommt `routing_used BOOLEAN NOT NULL DEFAULT FALSE`, `routing_reason TEXT NULL`, `routing_picked_model TEXT NULL`. Index `ix_ab_experiments_routing` on `(experiment_id, variant, routing_used)` für fitness-queries.
- **Context-Threading:** `AgentExecutionContext` gets optional `ab_row_id: str | None`. Dispatcher setzt per `dataclasses.replace(ctx, ab_row_id=row_id)` bevor runner invoked wird.
- **State-Threading:** `AgentGraphState.ab_row_id: str`, runner.py hydriert aus ctx.
- **Dispatcher:** neue `_mark_routing(row_id, routing_used, routing_reason, routing_picked_model)` fire-and-forget UPDATE (analog `_mark_fallback`).
- **llm_node:** bei routing-decision auf iteration==0 mit `ab_row_id` gesetzt wird `_mark_routing` via `asyncio.create_task` gefeuert. Span-attrs erweitert: `llm.routing_reason` (war da) + `llm.routing_used` (neu) + `llm.routing_picked` (neu).
- 4 neue Tests: empty-row-id noop, writes all 3 columns, swallows DB errors, dispatcher threads `ab_row_id` into ctx.

## Ref-Projekt-Bezug

| Datei | Basis | Anpassung für matrix |
|---|---|---|
| `agent/llm/smart_routing.py` | Port aus `_ref/hermes-agent/agent/smart_model_routing.py` (CLI-coding-only) | Multi-domain keyword set (trading/research/data/ML) + **bilingual EN+DE (matrix-only Erweiterung)** + hyphen-tokenizer (matrix-only) |
| `agent.ab_experiments` Schema | Matrix-native (exec-17 / Migration 025), kein Ref | — |
| ADR-001 Format | Eigenkreation, orientiert am Contrarian-Output | — |

**Hermes-Projekt (`_ref/hermes-agent/`)** ist der Haupt-Referenzpunkt für smart-routing. Die matrix-Adaption ist konservativer (engere Limits, mehr Guardrails) und bilingual. Spec-Alignment läuft noch (P1 Refactor + Update von `exec-a2fm-adaptive-routing.md`).

## Offene Punkte (Handoff)

### Smart-Routing-Rollout Gate — noch 3 offen

1. **G5 User-Visible Routing Indicator** (`#88`, M/1-2d, **frontend**)
   - Backend-Teil: `message_metadata` stream-packet könnte `routing_used`/`routing_picked` mittragen.
   - Frontend: Indicator in `frontend_merger/src/features/agent/` (z.B. kleines Icon pro message mit Tooltip "answered by cheap-model gpt-4o-mini"). GDPR-relevant.
2. **G6 Control-UI Panel** (`#89`, M/2-3d, **frontend**)
   - `frontend_merger/src/features/control/` — Toggle + cheap-model-dropdown + threshold-sliders für `smart_routing` JSONB.
3. **P1 Inversion — router_node.py** (`#90`, M/1d, **backend**)
   - Refactor smart-routing decision aus `llm_node.py`-side-effect in eigenständigen `agent/graph/nodes/router_node.py`.
   - Produziert `routing_decision` state-key.
   - Löst das iteration==0 semantisch sauber (als separate node, nicht als side-effect).
   - Re-used die `smart_routing.py` heuristik as-is.

**Empfohlene Reihenfolge** (aus ADR): P1 (backend, 1d) → G5 (frontend, 1-2d) → G6 (frontend, 2-3d, parallel zu G5). Nur dann `enabled: true` pro User.

### Weitere pendende Cluster-H Tasks (in open-tasks.md)

- `#66` exec-17 C9 ADR — tracing+audit parallel stores (~1.5h, backend-only)
- `#67` exec-harness §4g.4 TODOs + pareto dashboards (~3h)
- `#68` exec-hermes P6 frontend tail (~2h, frontend)
- `#69` exec-a2fm ML-router research (~3h, research)

## Lessons / Surprises

- **DE-keyword bias war nicht obvious** aus dem Contrarian-output alleine — erst durch concrete example walk-through ("analysiere mein Portfolio") klar dass die heuristic systematically inverted für den target-user.
- **`state["model"]` mutation im llm_node ist nicht idempotent** — credentialpool.acquire wird NACH der mutation mit cheap_provider aufgerufen. Ohne G2 wäre das ein silent 401 für jeden Anthropic-only User. Classic side-effect-order bug, nur sichtbar wenn man end-to-end walks through.
- **A/B Harness confounding** war der fundamentalste Finding — der "fitness data" für runner-variant-comparison wäre während rollout unbrauchbar gewesen ohne die routing-dimension auf demselben row. Recording the dimension musste BEFORE data collection passieren, nicht post-hoc.
- **AgentExecutionContext ist `frozen=True`** — `dataclasses.replace` war der richtige Weg, nicht mutable ctx oder ein separates thread-local.
- **`podman-compose` + `gitnexus analyze` parallel** — reindex läuft gerade (background task `bui37spi0`), ~3min. Danach sollte G4-Refactor impact sauber sichtbar sein.

## Nächster Step (wenn die Session fortgesetzt wird)

Smart-routing Backend ist **komplett abgeschlossen** (G1-G4 + P1 + ADR-001 + ADR-002).
Cluster H teilweise abgeschlossen (§C9 ADR-002 done).

Optionen für Fortsetzung:

**(A) Frontend gates G5/G6** — `#88` user-visible routing indicator + `#89` Control-UI panel. Braucht Browser-Testing + dev-stack. GDPR-relevant bevor `enabled: true` rollout möglich.

**(B) `#75` exec-14-DSPy contrarian review (stakes=high, ~2h)** — 5-7 decisions D-1..D-7 gate-blocked, D-1 "replace matrix skill-optimization durch DSPy?", D-7 "GEPA vs LLMSelector vs p1 als primary optimizer?". Gleicher Workflow wie ADR-001/002. Liefert Written Decision. No code — reine Architekturdebatte.

**(C) `#67` Cluster H exec-harness §4g.4 TODOs + pareto dashboards (~3h)** — konkrete code-work auf harness + fitness-dashboards.

**(D) `#69` exec-a2fm ML-router research (~3h, research)** — Nachfolger zu P1 Phase-1. Paper-review A2FM + LLMSelector + freerouter, schreib up welche Direction Phase-2 nimmt.

**(E) Weitere Cluster-Tasks** aus open-tasks.md Welle B/C/D.

---

## Final session status (prod-build smoke verified)

User-suggestion `bun run build && bun start` umging alle dev-bundle timing issues. End-to-end visual verify in production-frontend:

- **/control/api → SmartRoutingSection:** switch `checked=true`, disclosure-banner sichtbar, cheap-model input `value="anthropic/claude-haiku-4-5"`, max_simple_chars 140 (aus DB), max_simple_words 28, Save button.
- **Chat-Drawer via Top-Bar `Agent` Button:** öffnet Sheet-dialog sofort, AgentChatPanel rendert.
- **CompressionIndicator im header:** aria-label `Context compression: normal`, title-tooltip `Context usage normal — plenty of room.`, text `0%`, visible + clickable.
- **Full E2E roundtrip DB → Python → Go → BFF → React state → DOM:** grün.

**Smart-Routing Rollout-Gate: 100% komplett, ready-to-flip** sobald einer der folgenden eintritt: (a) German DE keyword corpus validated against real sessions, (b) per-user opt-in vom user selbst via `/control/api` panel, (c) contractual ready für silent model-substitution (ADR-001 disclosure banner macht das explizit).

**Offen für nächste Session:** `gh auth login -h github.com` + `git push` (2 local commits `968f432` + `fc279b5`), dann freie task-wahl.
